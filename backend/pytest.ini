[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    acceptance: end-to-end acceptance suite covering challenge-level workflows (auth, dashboard, network, hotspot, prediction, resilience)
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*sklearn\.utils\.parallel\.delayed.*:UserWarning
