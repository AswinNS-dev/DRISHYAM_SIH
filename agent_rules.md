# 🤖 Agent Rules (Mandatory)

You are working on **ONLY this GitHub Issue**.

## Scope

- Implement ONLY the functionality described in this issue.
- Do NOT modify unrelated features.
- Do NOT redesign existing modules.
- Do NOT refactor files outside the scope of this issue.
- Do NOT rename files, folders, APIs, models, variables, or components unless absolutely required for this issue.
- Do NOT change project architecture.

---

## Allowed Changes

You may modify any layer of the project ONLY if required to complete this issue.

This includes:

- Database
- Backend
- Frontend
- AI/ML
- API
- Models
- Services
- Documentation
- Tests

However, every change must directly support the issue requirements.

---

## Do NOT Modify

Unless explicitly required, avoid changing:

- Authentication flow
- User roles (RBAC)
- Navigation
- Sidebar
- Routing
- Global layouts
- Shared UI components
- Project structure
- Environment configuration
- Build configuration
- Dependencies
- Docker
- CI/CD

---

## Shared Files

If this issue requires changes to shared files such as:

- backend/app/api/v2.py
- datathon/src/services/api.ts
- App.tsx
- Navigation
- Sidebar

Only make the **minimum required additions**.

Never refactor or reorganize these files.

---

## Coding Standards

- Follow the existing project architecture.
- Reuse existing utilities and services.
- Keep naming conventions consistent.
- Maintain backward compatibility.
- Avoid duplicate code.
- Write clean, modular code.

---

## Before Writing New Code

Always check whether the functionality already exists.

If it exists:

- Extend it.
- Reuse it.

Do NOT create duplicate implementations.

---

## Forbidden Actions

Do NOT:

- Reformat the entire project.
- Change unrelated imports.
- Update unrelated dependencies.
- Fix unrelated bugs.
- Remove existing functionality.
- Replace existing libraries.
- Rewrite working code.
- Change API contracts unrelated to this issue.

---

## Deliverables

Only implement the requested feature.

After implementation provide:

1. Summary of files changed.
2. APIs added/modified.
3. Database changes (if any).
4. Frontend changes.
5. AI/ML changes (if any).
6. Testing performed.
7. Any follow-up work required.

---

## Success Criteria

A pull request should contain only changes necessary for this issue.

If a change is unrelated to the issue, DO NOT include it.