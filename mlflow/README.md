# Saksha MLflow Artifacts

This directory stores:
- registry manifests
- dataset version snapshots
- training metrics
- deployed artifact pointers

The implementation is filesystem-backed so it works in local development and CI without requiring a live MLflow server.
